[package]
name = "smt"
version = "0.1.0"
edition = "2024"

[dependencies]
utils = { path = "../utils", features = ["serde"] }
logic_formula = {path = "../logic_formula", features = ["serde"]}

[lints]
workspace = true
