use crate::{formula::formula::ARichFormula, environement::environement::Environement};

fn preprocess<'bump>(
    assertion: ARichFormula<'bump>,
    env: &Environement<'bump>
) -> ARichFormula<'bump> {
    todo!()
}